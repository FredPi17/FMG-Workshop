{% macro display_collectionImage(collection) %}
    {% import _self as macro %}

    {% for item in collection %}
        {{ macro.display_item_collectionImage(item) }}
    {% endfor %}
    {% if collection.vars.prototype is defined %}
        <div id='prototype-contenuImage'
             data-prototype='{{ macro.display_item_collectionImage(collection.vars.prototype)|e }}'>
        </div>
    {% endif %}
{% endmacro %}

{% macro display_item_collectionImage(item) %}
    <div class="un-contenu">

        Image
        <br>
        <span class="info">
            {{ item.imageFile.vars.data.intitule | default('') }}
            {{ form_widget(item.imageFile) }}
        </span>
        <br>
        <br>
        <button type="button" class="delete-contenu btn btn-primary btn-sm">Supprimer</button>
        <br>
        <br>
    </div>
{% endmacro %}
